# shared contracts

This directory stores the cross-runtime contracts between the Python host and the Node engine.

The contract must stay smaller and more stable than the underlying implementations.
