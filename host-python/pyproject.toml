[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "book-hermes-host"
version = "0.1.0"
description = "Hermes-derived Python host runtime for Book Hermes Agent"
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-dir = "tests"