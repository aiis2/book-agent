# host-python tests

Planned test types:
- bridge smoke tests
- tool contract tests
- request and response normalization tests
- host-side failure mapping tests

The first executable test should prove that the Python host can call the Node engine adapter and receive a structured response.
